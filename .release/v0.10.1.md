## Summary
- Release 0.10.1 (stable)
- Built from v0.10.0...v0.10.1
- Published intentionally from main via tag promotion

## Highlights
- fix(settings): gate Anthropic spend warning on api-key auth
- fix(tts): install torchaudio alongside torch from CUDA wheel index
- fix(hooks): skip version guard for merge commits
- feat(flywheel): merge-queue ordering with conflict detection
- chore(state): update spec for PAN-1221 (status=completed)
- PAN-1221 (#1225)
- feat(flywheel): session continuity via claude --resume on flywheel resume
- chore(state): post-release spec timestamp bump

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @panctl/cli
```

## Full changelog
- https://github.com/eltmon/panopticon-cli/compare/v0.10.0...v0.10.1
