## Summary
- Release 0.10.2 (stable)
- Built from v0.10.1...v0.10.2
- Published intentionally from main via tag promotion

## Highlights
- fix(cloister): self-heal unhealthy workspace stack in orphan-test recovery
- fix(contracts): make FlywheelPipelineItem.conflictsWith readonly
- fix(flywheel): self-heal stuck active-run gate (PAN-1245)
- fix(tts): read request body via Effect, add pan tts test --status

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @panctl/cli
```

## Full changelog
- https://github.com/eltmon/panopticon-cli/compare/v0.10.1...v0.10.2
