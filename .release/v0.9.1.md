## Summary
- Release 0.9.1 (stable)
- Built from v0.8.20...v0.9.1
- Published intentionally from main via tag promotion

## Highlights
- fix(infra): install npm 11 to a separate prefix in CI
- chore: release 0.9.1
- fix(infra): use OIDC trusted publishing instead of NPM_TOKEN
- chore: release 0.9.1
- fix(cli): commit .release/<tag>.md so CI can read it from the tagged commit
- chore: release 0.9.1
- fix(cli): align two stale tests with current architecture
- fix(infra): make pre-push hook POSIX-compliant for /bin/sh
- fix(infra): cross-platform builds + tag/version guard for release pipeline
- feat: track PAN-913 PAN-977 PAN-1069 continues and spec files
- fix(cloister): align reviewer session naming with spawnRun output (PAN-1048)
- fix(dashboard): also match legacy specialist-* naming in parseSpecialistSession
- fix(dashboard): match new agent naming pattern in parseSpecialistSession
- docs: add Planning Modes section documenting --auto flags
- chore(pipeline): commit beads + .pan/continues state from session 933
- PAN-1071 (#1099)
- chore(architecture): empty .claude/agents/; docs updated for no-ambient-subagents policy
- chore(scope): propose PAN-608 vBRIEF
- fix(cli): enforce pan wrapper drift guard (#1074)
- PAN-1094 (#1097)
- PAN-1092 (#1098)
- PAN-1093 (#1095)
- PAN-1091 (#1096)
- chore: gitignore graphify-out + fix stale 'pan specialists done' in roles/review.md
- PAN-1059 (#1089)
- fix(inspect): honor claude.permissionMode in inspect subagent spawn (PAN-1082)
- chore(pan-1059): remove stale vBRIEF and PRD from pre-PAN-967 legacy paths
- fix(roles): drop hardcoded model: opus from roles/review.md frontmatter
- fix(workspace): gitignore .pan/spec.vbrief.json + .pan/continue.json (PAN-1073)
- docs(skills): fix CLI drift in pan-plan, pan-admin-cloister, pan-review (PAN-1072)
- docs(skills): canonicalize the pan-* skill ↔ pan CLI convention (PAN-1072 partial)
- fix(permissions): close DSP leak paths on remote VMs; add lint guard
- chore: remove stale review-prompts paths from cost script
- feat(dashboard): add harness selector to restart agent dropdown
- fix(dashboard): carry pendingAssistantId across incremental JSONL parses
- refactor(cloister): finish PAN-967 — migrate continue files to .pan/continues/
- fix(prompts): point planning + work prompts at canonical .pan/ paths
- docs(vbrief): correct the four-artifact state model and retire legacy paths
- refactor(review): move convoy prompts to harness-agnostic role templates
- fix(review): align prompts with convoy contract
- feat(PAN-1059): runId SHA suffix, stalled-vs-done fix, test suite for review monitor and context
- fix(review): update orphan cleanup for PAN-1059 convoy session pattern
- feat(PAN-1059): convoy reviewers in isolated sessions, synthesis polls output files
- fix: convert sync FS to fs/promises in review-context and review-monitor
- feat(review): add convoy stall detection and session-reaper Stop hook
- feat(review): add review context manifest builder and spawnRun sub-role support
- docs(review): add PAN-1059 vBRIEF — review pipeline refactor plan
- feat(cloister): role primitive migration — ship role owns readyForMerge (PAN-1048)
- docs: add PAN-1059 PRD and launcher flow diagram
- chore(beads): update bead state for PAN-1048 progress
- fix(dashboard): render Pi conversation messages, refine Codex banner heuristic
- fix: harness UX, Pi conversation TUI, Codex banner, OpenAI model cleanup (PAN-1067)
- fix(dashboard): use JSONL-based diffs for all conversations including agents
- fix(dashboard): bind conversation harness to the conversation itself, not localStorage (PAN-1055)
- fix(dashboard): pi-extension build, harness gating, picker order, openai cleanup (PAN-1055)
- feat(dashboard): t3-inspired two-panel model picker with provider sidebar (PAN-1055)
- fix(dashboard): replace π glyph with readable "Pi" pill badge in picker button (PAN-1055)
- refactor(dashboard): segmented control harness picker + label ellipsis (PAN-1055)
- fix(dashboard): harness UX polish and pi-extension guard (PAN-1055)
- PAN-1055 (#1056)
- fix(dashboard): re-key orphan tool_use UUIDs for diff badge matching
- fix(dashboard): re-key file edits when tool_use and text entries merge
- chore(beads): append PAN-1044 workspace bead records
- fix(review): remove contradictory 'read-only' body text from pan-review-agent.md
- fix(review): remove 'read-only' from pan-review-agent description
- PAN-1044 (#1058)
- chore(main): clean pre-flywheel state after reboot
- fix(tmux,dashboard): close two infrastructure gaps from flywheel run 16
- fix(dashboard): guard against undefined agentId in activity feed filter
- docs(flywheel): update state snapshot after run 16
- fix(cloister): teardown prunes specialist registry entries for closed issue
- chore(main): sweep main hygiene before flywheel run
- fix(cli): teardown sweeps all workspace name variants, not just canonical
- feat(dashboard): add ReviewPipelineSection to InspectorPanel for cycle counter parity
- fix(agents): always pass --model to claude --agent so state.json wins over frontmatter
- fix(close-out): wire checkpoint-ref pruning into the lifecycle teardown
- fix(dashboard): remove fake turnId 'conversation-diff' blocking diff badges
- fix(close-out): teardown matches canonical session names
- fix(review): grant Write tool to review specialists for output files
- chore(dashboard): improve checkpoint prune logging
- feat(checkpoints): prune checkpoint refs at merge and close-out
- chore: clean up README test comment
- fix(dashboard): fall through to JSONL path in diffs/:turnId when no base commit
- fix(dashboard): resolve conv diffs by numeric ID + fix turn patch + fix scroll-to-bottom
- fix(dashboard): diff conversation turns against pre-conversation base commit
- fix(dashboard): fall through to JSONL per-turn diffs when in-repo base commit is missing
- fix(checkpoints): namespace checkpoint refs per-agent to fix diffs feature
- docs(infra): self-modify permission handling PRD
- fix(merge): check GitHub API before diff fallback for squash-merged PRs
- PAN-1024 (#1057)
- fix(review): mandate Write-tool invocation in reviewer + synthesis prompts
- fix(review): clear input buffer before resume-paste of reviewer prompt
- fix(review): reap reviewer zombies on coordinator death
- fix(feedback): never block feedback delivery on continue-state errors + auto-recover legacy specs
- docs(flywheel): update state snapshot — false-merge substrate fixes + 5 reopened issues
- fix(deacon): replace regex-based squash-merge detection with GitHub API truth
- fix(lifecycle): drop prior review-temp stash + refuse close-out when PR is open and unmerged
- fix(dashboard): harden awaiting-input prompts
- fix(dashboard): preserve stopped input prompts
- fix(dashboard): surface planning input in inspector
- fix(dashboard): bound awaiting-input pane scans
- fix(workspace): restore PAN-1030 vBRIEF
- chore(skills): track pipeline status local skill
- feat(dashboard): surface awaiting input prompts
- fix(agents): apply --dangerously-skip-permissions to resume launchers when yolo mode is bypass
- fix(settings-overlay): use valid Claude Code permission syntax + strip legacy patterns
- fix(channels): import bridge runtime helpers
- fix(dashboard): narrow review button status types
- fix(review): keep failed coordinators inspectable
- fix(dashboard): surface stranded pending reviews
- fix(cloister): route verification failures back to review
- fix(review): retry timed-out specialists
- fix(deacon): never nudge agents with state='active' as idle
- fix(agents): inject Panopticon-infra permission deny rules at every spawn
- docs(scope): defer x1-delete-old-pan-agent-md bead in PAN-1048 spec
- fix(planning): use conventional-commits format for auto-generated planning commits
- docs(workspace): promote PAN-1048 vBRIEF to .pan/specs/
- fix(agents): always send a continue prompt on resume, not just when message supplied
- fix(cloister): stamp merged:true on agent state and refuse to auto-resume merged agents
- fix(review): pin remain-on-exit immediately after new-session, before claude can exit
- fix(cloister): avoid false merged reconciliation
- chore(beads): sync active task state
- fix(infra): untrack Panopticon runtime files
- fix(deacon): reconcile test_status alongside review_status when merge=merged
- chore(beads): sync test task export
- fix(merge): verify branch landed before cleanup
- chore(beads): sync workspace task export
- fix(dashboard): make agent ceilings advisory
- fix(deacon): reconcile review_status=reviewing when merge_status=merged
- fix(deacon): emit thinking_start from user-prompt-submit, drop pane-scrape gate
- fix(deacon): skip first-completion nudge when pane shows active Claude status
- fix(dashboard): allow direct work starts without PRD
- fix(cloister): add reverse-direction reconcile for stale mergeStatus=merged
- feat(dashboard): expose Start Planning in right-click + kanban for stuck issues
- fix(dashboard): reopen completed planning in dialog
- fix(cloister): run postMergeLifecycle from deacon reconciliation paths
- fix(dashboard): refuse start-agent when no beads exist (PAN-977 unblock)
- fix(review): respawn specialist when pane is dead instead of crashing
- docs(flywheel): initial FLYWHEEL-STATE.md for run 2026-05-09
- fix(cli): align MiniMax runtime auth with settings
- fix(review): use dashboard API for terminal status + atomic reviewedAtCommit snapshot
- fix(review-agent): cd to workspace in coordinator bash -lc command (PAN-1034)
- fix(workspace): resolve numeric-suffix workspace first (PAN-1034 follow-on)
- fix(review-agent): spawn specialist sessions atomically (PAN-1034)
- docs(flywheel): delete FLYWHEEL-STATE.md — starting fresh next run
- docs(flywheel): update state snapshot for Run 16
- chore(beads): sync task state after agent work sessions
- fix(graphify): replace full report load with 700-token summary
- feat(graphify): add LLM-labeled communities to knowledge graph
- fix(graphify): add community labels to graph.html
- feat(graphify): add knowledge graph for codebase (8830 nodes, 20525 edges)
- fix(dashboard): restore conversation creation
- feat(CommandDeck): show inactive projects section for registered projects with no active issues
- PAN-1025 (#1026)
- PAN-987 (#1023)
- PAN-1028 (#1033)
- fix(dashboard): keep conversation favorites in sync
- PAN-1015 (#1043)
- fix(cloister): surface reviewer terminal API errors instead of silent timeout
- fix(db): self-heal SQLite to incremental_vacuum on startup
- perf(db): cap SQLite WAL size + periodic TRUNCATE checkpoint
- perf(triggers): mtime-cache checkTaskCompletion to stop disk thrashing
- feat(cloister): deacon nudges idle work agents with open beads
- fix(cloister): hoist keep-going imperative out of REMOTE gate
- fix(infra): stop-hook logs first-thing for diagnostic visibility
- fix(dashboard): pick freshest JSONL by mtime in resolveClaudeSessionId
- fix(dashboard): hoist useMemo above early returns in PlanDAGViewer
- docs(beads): flip PAN-1015 vBRIEF status proposed -> active
- feat(planning): conditional per-bead inspection via metadata.requiresInspection
- fix(dashboard): await initEventStore() in startTtsSummarizer
- docs(beads): canonical .pan/specs/ entry for PAN-1015
- docs(beads): plan PAN-1015 — claudish removal vBRIEF + 12 beads
- fix(infra): correct gemini-3-flash → gemini-3-flash-preview model ID
- fix(cloister): log global pause skip once
- test(agents): pin permissionMode=bypass in auth-routing mocks
- fix(security): stop --dangerously-skip-permissions leak when user has selected Auto
- fix(cli): pan recover honors bypass + supports --model + planning sessions
- docs(beads): plan PAN-1029 — harness picker UI PRD, vBRIEF, beads
- feat(skills): pipeline-status — cross-room visual status board

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @panctl/cli
```

## Full changelog
- https://github.com/eltmon/panopticon-cli/compare/v0.8.20...v0.9.1
