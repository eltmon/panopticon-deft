# Operation Fix-All (consolidated)

This document has been folded into [`flywheel-brief.md`](./flywheel-brief.md).

The Flywheel no longer appends run reports to this file. Durable cumulative memory now lives in [`FLYWHEEL-STATE.md`](./FLYWHEEL-STATE.md), owned by the orchestrator. The file does not exist yet — it is created on the first run that records something worth remembering across runs.

Historical run reports that used to live here are intentionally not seeded forward.

The original content is preserved in git history.
