# Panopticon 1.0 Audit Results (Epic D)

**Status:** Dispositions complete, awaiting user decision on stash@{18}
**Date:** 2026-04-23
**Related:** [1.0-stabilization-plan.md](./1.0-stabilization-plan.md), GitHub #804

## Methodology

For each flagged item: read the stash/commit content, identify touched files, compare against HEAD by searching for marker symbols/functions, diff against HEAD for stash content, and classify disposition.

## Flagged items — 9 total, 6 unique (3 duplicates)

| # | Item | Label | Disposition |
|---|---|---|---|
| 1 | `stash@{34}` + dangling `d81260d0e` | orphaned-pan-685-model-override-frontend | **FULLY INTEGRATED** |
| 2 | `stash@{38}` | orphaned-material-symbols | **FULLY INTEGRATED** |
| 3 | `stash@{35}` | orphaned-pan-tts-and-model-routing-readme | **MOSTLY INTEGRATED** (2 minor drift items — see below) |
| 4 | `stash@{39}` + dangling `df0982bcb` | orphaned-model-override-work | **FULLY INTEGRATED** |
| 5 | Dangling `7b12b85990` | On main: other changes to restore later | **FULLY INTEGRATED** |
| 6 | `stash@{18}` | frontend: inspector panel changes (agent leakage, needs triage) | **DRIFT-HEAVY — needs user decision** (see below) |

## Per-item verification

### 1. PAN-685 model-override frontend (`stash@{34}` + `d81260d0e`)

- Touched `label-cleanup.ts` and several frontend components.
- HEAD has `repairIncompletePostMergeLifecycle` at `src/lib/lifecycle/label-cleanup.ts:187`.
- Boot path in `src/dashboard/server/main.ts:154` is already commented out.
- **Verdict:** Work integrated; the boot-path repair pattern that this introduced is the exact pattern Epic A will replace.

### 2. Material Symbols (`stash@{38}`)

- HEAD uses Material Symbols with a superset of icons.
- **Verdict:** Fully integrated.

### 3. PAN-TTS + model-routing README (`stash@{35}`)

- TTS skill content present in HEAD.
- Merge-agent related changes present.
- **Minor drift:**
  - `pan-tts/SKILL.md` still references `pan start PAN-XXX` (legacy) instead of `pan work issue PAN-XXX`.
  - `WORK-TYPES.md` remains in tree despite the orphan proposing deletion.
- **Verdict:** Integrated. The two drift items are cosmetic and belong in normal doc maintenance, not Epic D blocking.

### 4. Model-override work (`stash@{39}` + `df0982bcb`)

- HEAD has `requiresClaudish`, `getOpenAIAuthStatus`, and all claudish routing logic.
- Files diff ~445 lines vs the orphan's content, meaning the work evolved substantially after the orphan was set aside.
- **Verdict:** Fully integrated.

### 5. "Other changes to restore later" (`7b12b85990`)

- Contracts (`contracts/events.ts`, `contracts/event-reducers.ts`) present in HEAD as supersets.
- `ActivityPanel.tsx` evolved 502 lines beyond the orphan.
- All listed files (merge-agent, work-agent-prompt, store, App, event-store, pending-lifecycle, metrics, domain-services) present and extended in HEAD.
- **Verdict:** Fully integrated; the orphan was a superseded snapshot.

### 6. Inspector panel (`stash@{18}`) — **NEEDS USER DECISION**

Stash label: `frontend: inspector panel changes (agent leakage, needs triage)`.

Net diff vs HEAD: **+298 / -505** across 8 files. Appearance of simplification is misleading — the stash is an older base that lacks features HEAD added afterward.

**Regressions if integrated as-is (features HEAD has, stash removes):**
- `inline` prop on `DetailPanelLayout` (embedded mode)
- `embedded` prop on `InspectorPanel`
- Pinned-session validation effect in `DetailPanelLayout`
- `planningState` query in `InspectorPanel`
- Ready-to-start sort in `KanbanBoard` (`todoPlanningStates`, `planningCompleteById`, `sortedGrouped`)
- Shared components: `MergeButton`, `RecoverButton`, `StopAgentButton`, `ResetIssueButton`, `ArtifactLinks` (stash inlines merge/reset mutations into `InspectorPanel` — anti-DRY)
- DnD re-enabled in `KanbanBoard` — but HEAD deliberately disabled this ("pending rework, PAN-TODO")

**Real improvements only in stash (2 items):**
1. **Event-backed review status in `DetailPanelLayout`**: stash uses `useDashboardStore(selectReviewStatus(issueId))` with polled fallback and `refetchInterval: eventReviewStatus ? false : 15000`. HEAD already imports `selectReviewStatus` in `KanbanBoard.tsx` and populates `reviewStatusByIssueId` — but the Inspector-side panel still polls every 15s. This is a real latency win that's partially-but-not-fully integrated.
2. **Unknown-phase diagnostic UI**: `showUnknownPhaseDiagnostic` in `InspectorPanel` shows containers count + lifecycle reason when phase is unknown. 2 occurrences in stash, 0 in HEAD. Useful for debugging.

**Recommendation: DROP the stash, file two small follow-up issues.**

Rationale:
- The "agent leakage" label marks this as known off-track work.
- Surgically extracting the two wins is a better path than merging a stash that would regress 8 HEAD features.
- Both wins are <1-day tickets on their own.

## Summary

- **5 of 6 unique items fully integrated** — safe to drop classified stashes.
- **stash@{18}** needs user decision: drop + file two follow-up tickets (recommended), OR manually integrate the 2 wins and reject the regressions.

## Follow-up tickets to file (if user agrees with recommendation)

1. `Inspector: use event-backed review status instead of 15s polling` — extend the `selectReviewStatus` pattern already in `KanbanBoard.tsx` to `DetailPanelLayout.tsx`.
2. `Inspector: unknown-phase diagnostic UI` — show containers count + lifecycle reason when `phase` is `unknown` to aid debugging.

## Remaining Epic D work (after stash@{18} decision)

- Drop the ~100 auto-classified stashes (pre-merge-backup / wip-auto / spawn-stash / retry-debris / release-scratch / temp-pan-*-review-request-*).
- Branch triage: push-or-abandon 7 ahead-of-remote branches (see stabilization PRD).
- Reconcile local `main` with `origin/main`.
- Document stash-hygiene policy in `CLAUDE.md`.
- Verify `git gc` won't lose anything valuable after cleanup.
