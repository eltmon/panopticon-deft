## Pass-3 deep audit — 2026-05-24

**Verdict**: REOPEN

### Original AC (verbatim quote from issue body)
- [ ] Zero `Promise<T>` return types in `src/lib/**/*.ts` production files (tests can still use whatever they need)
- [ ] Zero `*Async` / `*Promise` suffix exports in `src/lib/`
- [ ] All Effect variant names canonicalized (no more `*Effect` suffix; that's the only API)
- [ ] `grep -rL "from ['\"]effect" src/lib --include="*.ts" | grep -v test` finds no production files
- [ ] Full project typechecks clean
- [ ] Full test suite passes

### Per-AC verification
1. **Zero `Promise<T>` return types in `src/lib/**/*.ts` production files (tests can still use whatever they need)**
   ✗ NOT MET  
   *Evidence*: At audit target `c173152f7`, production `src/lib/**/*.ts` still contains 643 `Promise<...>` markers:
   ```text
   $ git grep -n "Promise<" c173152f7 -- 'src/lib/**/*.ts' ':!src/lib/**/__tests__/**' ':!src/lib/**/*.test.ts' ':!src/lib/**/*.spec.ts' | head -12
   c173152f7:src/lib/caveman/workspace.ts:58:export async function injectMemoryHookSettings(workspacePath: string): Promise<void> {
   c173152f7:src/lib/caveman/workspace.ts:81:}async function injectCavemanSettingsPromise(workspacePath: string, variant: CavemanVariant): Promise<void> {
   c173152f7:src/lib/caveman/workspace.ts:133:async function readWorkspaceSettings(settingsPath: string): Promise<Record<string, unknown>> {
   c173152f7:src/lib/caveman/workspace.ts:142:async function installTrustedMemoryHookScript(): Promise<string> {
   c173152f7:src/lib/caveman/workspace.ts:225:}async function readCavemanVariantPromise(workspacePath: string): Promise<CavemanVariant> {
   c173152f7:src/lib/channels/panopticon-bridge.ts:187:  forwarder: (agentId: string, request: ChannelPermissionRequest) => Promise<void> =
   c173152f7:src/lib/channels/panopticon-bridge.ts:189:): Promise<void> {
   c173152f7:src/lib/channels/panopticon-bridge.ts:203:    _notificationHandlers?: Map<string, (notification: unknown) => Promise<void>>;
   c173152f7:src/lib/channels/panopticon-bridge.ts:279:): Promise<ChannelPushResult> {
   c173152f7:src/lib/channels/panopticon-bridge.ts:316:): Promise<ChannelPushResult> {
   c173152f7:src/lib/channels/panopticon-bridge.ts:355:): Promise<void> {
   c173152f7:src/lib/channels/panopticon-bridge.ts:398:async function appendBridgeLog(agentId: string, entry: Record<string, unknown>): Promise<void> {
   COUNT=643
   ```

2. **Zero `*Async` / `*Promise` suffix exports in `src/lib/`**
   ✓ VERIFIED  
   *Evidence*: At audit target `c173152f7`, no production `src/lib/**/*.ts` export declarations matched `*Async` or `*Promise`:
   ```text
   $ git grep -nE "\bexport\s+(async\s+)?(function|const|let|var|class|interface|type|enum)\s+\w*(Async|Promise)\b|\bexport\s*\{[^}]*\w*(Async|Promise)\b" c173152f7 -- 'src/lib/**/*.ts' ':!src/lib/**/__tests__/**' ':!src/lib/**/*.test.ts' ':!src/lib/**/*.spec.ts'
   COUNT=0
   ```

3. **All Effect variant names canonicalized (no more `*Effect` suffix; that's the only API)**
   ✓ VERIFIED  
   *Evidence*: At audit target `c173152f7`, no production `src/lib/**/*.ts` export declarations matched a `*Effect` suffix:
   ```text
   $ git grep -nE "\bexport\s+(async\s+)?(function|const|let|var|class|interface|type|enum)\s+\w*Effect\b|\bexport\s*\{[^}]*\w*Effect\b" c173152f7 -- 'src/lib/**/*.ts' ':!src/lib/**/__tests__/**' ':!src/lib/**/*.test.ts' ':!src/lib/**/*.spec.ts'
   COUNT=0
   ```

4. **`grep -rL "from ['\"]effect" src/lib --include="*.ts" | grep -v test` finds no production files**
   ✗ NOT MET  
   *Evidence*: In a detached worktree at `c173152f7`, the exact AC command still returns 64 production files:
   ```text
   $ grep -rL "from ['\"]effect" src/lib --include="*.ts" | grep -v test | head -20
   src/lib/subscription-types.ts
   src/lib/runtimes/index.ts
   src/lib/paths.ts
   src/lib/tracker/factory.ts
   src/lib/tracker/index.ts
   src/lib/safety/protected-paths.ts
   src/lib/shadow-engineering/index.ts
   src/lib/cloister/complexity.ts
   src/lib/cloister/stuck-remediation.ts
   src/lib/cloister/health.ts
   src/lib/cloister/stuck-remediation-state.ts
   src/lib/cloister/agent-idle.ts
   src/lib/cloister/auto-resume-config.ts
   src/lib/cloister/index.ts
   src/lib/cloister/router.ts
   src/lib/cloister/no-resume-mode.ts
   src/lib/planning/index.ts
   src/lib/graphify/refresh.ts
   src/lib/pan-dir/index.ts
   src/lib/pan-dir/types.ts
   COUNT=64
   ```

5. **Full project typechecks clean**
   ✗ NOT MET  
   *Evidence*: In a fresh detached worktree at `c173152f7`, after `bun install`, `npm run typecheck` exited non-zero:
   ```text
   $ npm run typecheck
   ...
   src/lib/conversations/transcript-summary.ts(24,34): error TS2307: Cannot find module '@panctl/contracts' or its corresponding type declarations.
   src/lib/flywheel-merge-order.ts(3,43): error TS2307: Cannot find module '@panctl/contracts' or its corresponding type declarations.
   src/lib/memory/checkpoints.ts(2,37): error TS2307: Cannot find module '@panctl/contracts' or its corresponding type declarations.
   ...
   src/lib/memory/worker-pool.ts(2,40): error TS2307: Cannot find module '@panctl/contracts' or its corresponding type declarations.
   EXIT=2
   ```

6. **Full test suite passes**
   ✗ NOT MET  
   *Evidence*: In the same detached worktree at `c173152f7`, `npm test` exited non-zero:
   ```text
   $ npm test
   ...
   Test Files  1 failed | 435 passed | 4 skipped (440)
   Tests  2 failed | 4752 passed | 50 skipped (4811)

   FAIL  |root| tests/lib/settings-api.test.ts > settings-api > getAvailableModelsApi > should include all anthropic models
   AssertionError: expected [ 'claude-opus-4-7', …(3) ] to include 'claude-sonnet-4-5'

   FAIL  |root| tests/lib/settings-api.test.ts > settings-api > getAvailableModelsApi > should include openai models as objects
   AssertionError: expected [ 'gpt-5.5', 'gpt-5.4', …(4) ] to include 'o3'

   EXIT=1
   ```

### UI reproduction (if any UI in the AC)
N/A — PAN-1313 has no UI acceptance criteria.

### Recommendation
- Reopen. AC1, AC4, AC5, and AC6 are not met on the audit target: production `src/lib/` still has hundreds of `Promise<...>` return/type markers, the exact Effect-import coverage command still returns 64 production files, typecheck fails, and the full test suite fails.
- Suggested reopen comment text:
  > Reopening after pass-3 audit. The original ACs are not satisfied at `c173152f7`: `src/lib/**/*.ts` production files still contain 643 `Promise<...>` markers, the exact `grep -rL "from ['\"]effect" src/lib --include="*.ts" | grep -v test` command still returns 64 production files, `npm run typecheck` fails in a fresh worktree, and `npm test` exits non-zero. The cleanup needs another pass against the original AC text, not proxy evidence.
