## Pass-3 deep audit — 2026-05-24

**Verdict**: REOPEN

### Original AC (verbatim quote from issue body)

### Phase 3a — Delete the legacy Promise variants

- **22 `*Async` exports** remain in `src/lib/`, across 8 files: `vbrief/vbrief-index.ts`, `vbrief/io.ts`, `vbrief/dag.ts`, `vbrief/continue-state.ts`, `pan-dir/specs.ts`, `pan-dir/continue.ts`, `memory/checkpoint-client.ts`, `cloister/config.ts`.
- **3 test files** still import `*Async` from `src/lib/`: `tests/lib/cloister/pan-464-container-health.test.ts`, `tests/unit/dashboard/server/routes/projects.test.ts`, `tests/unit/lib/lifecycle/teardown-workspace.test.ts` — migrate these to the `*Effect` siblings *first*, then the `*Async` exports can be deleted.
- Delete each `*Async` export and the dead `Effect.runPromise` bridges that only existed to span the Promise↔Effect boundary.
- Per-file: migrate any remaining test importers → delete `*Async` exports → `npm run typecheck`. A deletion that breaks typecheck means a caller was missed.

**Verification gate:** zero `*Async`-from-`src/lib/` imports anywhere in the repo; `npm run typecheck` + `npm run lint` + `npm test` all green.

### Phase 3b — Canonicalize naming

Once Phase 3a is verified clean (gate above passes), do the cosmetic rename:

- Rename every surviving `fooEffect` → `foo` across `src/lib/`, `src/cli/`, `src/dashboard/server/`, and the test suite. Effect is the canonical form; the `Effect` suffix was temporary scaffolding from PAN-1249's additive phase.
- Where a function has *both* an `*Effect` variant *and* a genuinely-needed sync variant of the same logical operation, the sync one becomes `*Sync` (e.g. `readPlanSync`). The Effect-returning one takes the plain name (`readPlan`).
- This is a codebase-wide mechanical rename — a single coherent commit using a scripted find-replace, then test fixups in the same commit.

**End state:** `src/lib/` exposes `foo()` (returns `Effect`) as the canonical name, `fooSync()` only where sync is genuinely needed. **No `*Async`, no `*Effect` suffix anywhere.**

### Per-AC verification

1. **22 `*Async` exports remain in `src/lib/`, across 8 files...**
   ✓ VERIFIED  
   *Evidence*: Command counted exports in the 8 named files:
   ```text
   ASYNC_EXPORTS_IN_PHASE3A_FILES=0
   ```

2. **3 test files still import `*Async` from `src/lib/`... migrate these to the `*Effect` siblings first...**
   ✓ VERIFIED  
   *Evidence*: `tests/unit/dashboard/server/routes/projects.test.ts:65-69` imports `fetchProjectSessionTree`, `listProjectsSync`, `listSessionNames`, `getAgentRuntimeState`, and fs promise helpers; no `*Async` import from `src/lib`. `tests/unit/lib/lifecycle/teardown-workspace.test.ts:49-55` imports `teardownWorkspace`, `sessionExists`, and wraps the Effect with `Effect.runPromise`; no `*Async` import from `src/lib`. `tests/lib/cloister/pan-464-container-health.test.ts:58-90` mocks Effect-returning tmux APIs; no `*Async` import from `src/lib`.

3. **Delete each `*Async` export and the dead `Effect.runPromise` bridges...**
   ✓ VERIFIED for the 8 Phase-3a files  
   *Evidence*: Command output:
   ```text
   ASYNC_EXPORTS_IN_PHASE3A_FILES=0
   EFFECT_RUNPROMISE_IN_PHASE3A_FILES=0
   ```

4. **Per-file: migrate any remaining test importers → delete `*Async` exports → `npm run typecheck`.**
   ⊘ AMBIGUOUS  
   *Evidence*: Historical per-file sequencing is not reproducible from current main. Current gate did pass after building contracts:
   ```text
   npm run build --workspace @panctl/contracts
   npm run typecheck
   > tsc --noEmit
   ```
   but this does not prove the required per-file sequencing happened.

5. **Verification gate: zero `*Async`-from-`src/lib/` imports anywhere in the repo.**
   ✓ VERIFIED  
   *Evidence*: Script scanning imports from `src/lib` reported:
   ```text
   TOTAL=0
   ```

6. **Verification gate: `npm run typecheck` + `npm run lint` + `npm test` all green.**
   ✗ NOT MET  
   *Evidence*: `npm run typecheck` passed after building `@panctl/contracts`, but `npm run lint` and `npm test` fail on current main.
   ```text
   npm run lint
   Exit code 1
   skill CLI lint failed:
     sync-sources/skills/pan-flywheel/SKILL.md:25: pan flywheel start: could not read help for pan flywheel start
     sync-sources/skills/pan-show/SKILL.md: pan show exists in top-level help but --help failed
     sync-sources/skills/pan-start/SKILL.md: pan start exists in top-level help but --help failed
     sync-sources/skills/pan-status/SKILL.md: pan status exists in top-level help but --help failed
     sync-sources/skills/pan-sync/SKILL.md: pan sync exists in top-level help but --help failed
   ```
   ```text
   npm test
   Exit code 1
   tests/lib/settings-api.test.ts:277 expected anthropicIds to contain 'claude-sonnet-4-5'
   tests/lib/settings-api.test.ts:294 expected openaiIds to contain 'o3'
   Test Files 1 failed | 435 passed | 4 skipped
   Tests 2 failed | 4752 passed | 50 skipped
   ```

7. **Rename every surviving `fooEffect` → `foo` across `src/lib/`, `src/cli/`, `src/dashboard/server/`, and the test suite.**
   ✗ NOT MET  
   *Evidence*: Current code still defines and consumes a project-defined `*Effect` API in `src/dashboard/server`:
   ```text
   src/dashboard/server/services/read-workspace-file.ts:150: export function readWorkspaceFileEffect(input: ReadWorkspaceFileInput): Effect.Effect<ReadWorkspaceFileResult, PanRpcError> {
   src/dashboard/server/ws-rpc.ts:34: import { readWorkspaceFileEffect } from './services/read-workspace-file.js';
   src/dashboard/server/ws-rpc.ts:676: readWorkspaceFileEffect(input),
   src/dashboard/server/services/__tests__/read-workspace-file-rpc.test.ts:15: import { readWorkspaceFileEffect } from '../read-workspace-file.js';
   src/dashboard/server/services/__tests__/read-workspace-file-rpc.test.ts:25: return Effect.runPromise(readWorkspaceFileEffect(input));
   ```

8. **Where a function has both an `*Effect` variant and a genuinely-needed sync variant... sync one becomes `*Sync`; Effect-returning one takes the plain name.**
   ✗ NOT MET  
   *Evidence*: `src/dashboard/server/services/read-workspace-file.ts:150` exposes the Effect-returning function as `readWorkspaceFileEffect`, not the plain name.

9. **This is a codebase-wide mechanical rename — a single coherent commit using a scripted find-replace, then test fixups in the same commit.**
   ⊘ AMBIGUOUS  
   *Evidence*: Git shows a single implementation commit, `a09d03c02 feat(lib): Effect migration Phase 3 — delete legacy *Async variants + canonicalize naming (PAN-1379)`, but current code proves the rename was not codebase-wide complete. Whether a scripted find-replace was used is not verifiable from current repo state.

10. **End state: `src/lib/` exposes `foo()` (returns `Effect`) as the canonical name, `fooSync()` only where sync is genuinely needed. No `*Async`, no `*Effect` suffix anywhere.**
   ✗ NOT MET  
   *Evidence*: `src/dashboard/server/services/read-workspace-file.ts:150`, `src/dashboard/server/ws-rpc.ts:34`, `src/dashboard/server/ws-rpc.ts:676`, and `src/dashboard/server/services/__tests__/read-workspace-file-rpc.test.ts:15,25,33` still contain `readWorkspaceFileEffect`. The explicit green-gate requirement is also currently failing via `npm run lint` and `npm test`.

### UI reproduction (if any UI in the AC)

No UI acceptance criteria in the original issue body.

### Recommendation

- Reopen PAN-1379.
- Specific unmet ACs: Phase 3b canonicalization is incomplete because `readWorkspaceFileEffect` remains in dashboard server code/tests, and the explicit verification gate is not green (`npm run lint` and `npm test` fail).
- Recommended reopen comment text:

```markdown
Reopening after pass-3 audit. The original PAN-1379 AC required codebase-wide `fooEffect -> foo` canonicalization across `src/lib`, `src/cli`, `src/dashboard/server`, and tests, plus a green `typecheck`/`lint`/`test` gate.

Current main still has a project-defined `*Effect` API:
- `src/dashboard/server/services/read-workspace-file.ts:150` exports `readWorkspaceFileEffect`
- `src/dashboard/server/ws-rpc.ts:34,676` imports/calls it
- `src/dashboard/server/services/__tests__/read-workspace-file-rpc.test.ts:15,25,33` imports/tests it

The required gate is also not green on current main:
- `npm run lint` fails in `scripts/lint-skills.sh`
- `npm test` fails in `tests/lib/settings-api.test.ts` model catalog assertions

Phase 3a deletion of the named legacy `*Async` exports appears complete, but Phase 3b/end-state AC is not met.
```
