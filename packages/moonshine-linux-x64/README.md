# moonshine-linux-x64

Optional Linux x64 package for the Panopticon Moonshine speech-to-text sidecar.

The `bin/moonshine-sidecar` binary is produced by `npm run build:sidecar` from the repository root.
